# 电梯百科展示网站（需求与版本规划）

## 前言

上电梯时想到的一个学习项目：做一个面向**普通好奇用户**的电梯“百科展示”网站，用少量 Demo 数据把卡片展示、筛选交互、组件化与路由等关键前端能力串起来。

---

## 1. 项目定位

### 目标用户

- 对电梯品牌/国家/类型感兴趣的普通用户（非专业采购、非工程设计）

### 核心价值

- 用“卡片 + 参数 + 图片/示意图”的方式快速对比不同国家/品牌的电梯型号与基础参数

### 不做什么（非目标）

- 不做登录/后台录入/权限系统
- 不做真实电梯选型/报价/合规性建议（仅百科式展示）
- 不做复杂搜索（V1/V2 不含全文搜索）

---

## 2. 功能总览（最终形态）

### 2.1 列表展示

- 电梯卡片包含：图片/示意图、国家、品牌、型号、类型、基本参数
- 基本参数（单位固定）：
  - 限载重量：`kg`
  - 额定速度：`m/s`
  - 轿厢尺寸：`宽/深/高 (m)`
  - 产地国（国家/国家代码）

### 2.2 筛选（V2 起）

- 支持按 **国家 / 品牌 / 类型** 三个维度筛选
- 默认均为“全部”，任一维度选择后按“同时满足（AND）”过滤
- 空结果时给出“无匹配结果”的提示，并提供一键清空筛选

### 2.3 详情（V2 起：Modal；V4 起：详情页）

- V2：点击卡片弹出 **Modal 弹窗** 展示更多详情
- V4：迁移为独立详情页路由（列表页 → 详情页）

### 2.4 数据方案

- 数据存放于本地 JSON 文件：`data/elevators.json`
- 前端读取并渲染（V1 可先静态，V2 起从 JSON 读取）

---

## 3. 数据范围（最小化 Demo）

- 国家：5 个（例如：日本、美国、德国、中国、芬兰）
- 品牌示例：三菱、奥的斯、迅达、蒂森克虏伯、通力（KONE）
- 类型示例：住宅梯、商用梯、观光梯、货梯
- 电梯总数：约 10~12 条记录

---

## 4. 数据结构（`data/elevators.json`）

### 4.1 文件结构

```json
{
  "elevators": [ /* ... */ ]
}
```

### 4.2 字段定义（建议作为“单一数据源”）

- `id`：number，唯一 ID（用于详情定位/路由参数）
- `brand`：string，品牌中文
- `brand_en`：string，品牌英文（可用于展示/排序/URL）
- `model`：string，型号
- `country`：string，国家中文
- `country_code`：string，国家代码（用于徽标/缩写）
- `type`：string，类型（枚举：住宅梯/商用梯/观光梯/货梯）
- `max_load_kg`：number，限载重量（kg）
- `speed_ms`：number，速度（m/s）
- `cabin_width_m` / `cabin_depth_m` / `cabin_height_m`：number，轿厢宽/深/高（m）
- `description`：string，补充说明（用于详情）

---

## 5. 版本交付边界（从 V1 开始逐步实现）

> 目的：把“最终愿景”和“当前可交付”拆开，保证每个版本都能独立验收。

### V1（静态页：HTML + CSS + Tailwind CSS）

**必须（Must）**

- 单页静态列表：能看到全部 Demo 电梯卡片（10~12 条）
- 卡片展示字段：国家、品牌、型号、类型、载重、速度、轿厢尺寸、产地国
- 响应式布局（移动端单列 → 桌面端多列）

**不做（Won’t）**

- 不从 JSON 动态读取/渲染
- 不做筛选、不做 Modal

**验收（Acceptance）**

- 直接打开 `v1/index.html`（或在落地到 `v1/` 前先用 `codex/index.html`）即可浏览；无控制台报错

### V2（交互：+ JavaScript + JSON）

**必须（Must）**

- 从 `data/elevators.json` 读取并渲染卡片
- 三维筛选（国家/品牌/类型）+ 清空 + 空结果提示
- 点击卡片打开 Modal，展示详情字段（至少包含 `description` 与全部参数）

**建议（Should）**

- Modal 支持 ESC 关闭、点击遮罩关闭、关闭按钮

### V3（组件化：+ TypeScript + React）

- 用 React 拆分组件（筛选栏/卡片/列表/详情）
- TypeScript 类型定义与数据校验（最小可用）
- 状态管理清晰（筛选状态、当前选中详情）

**运行说明（V3 特别重要）**

- V3 是工程化项目：不能像 V1/V2 一样双击 `index.html` 直接运行。
- 预览方式：
  - 开发：在 `v3/` 目录执行 `npm install`，再执行 `npm run dev`
  - 构建：在 `v3/` 目录执行 `npm run build`，然后打开 `v3/dist/index.html`

**本机公共技术栈（可选约定）**

- 本机把 React 19 + TypeScript 5 + Vite 7 的公共配置放在：`C:\frontend-stack`
- V3 会从该目录引用 `tsconfig` 与 `vite` 的公共配置（用于后续版本复用）

### V4（应用化：+ Next.js）

- 列表页 + 详情页路由
- SEO 基础（title/description、语义化结构、可抓取内容）
- 生成/渲染策略（SSG/SSR）按学习目标选择

**当前实现（2026-03-07）**

- 已完成列表页 `/` 与详情页 `/elevators/[id]`
- 已完成基础 SEO：全局 metadata + 详情页动态 metadata
- 已完成静态生成：详情页通过 `generateStaticParams` 做 SSG
- 已保留 V3 的国家 / 品牌 / 类型 / 关键词筛选
- 已将 Next.js 公共 TypeScript 配置抽离到 `C:\frontend-stack`

---

## 6. 交互与展示细则（供后续版本落地）

### 筛选（V2+）

- 每个维度默认“全部”
- 每个维度单选（先简单），多维度组合时取 AND

### 详情（V2+）

- Modal 内容建议包含：
  - 图片/示意图、品牌/型号、国家/类型
  - 参数表（载重、速度、轿厢尺寸、产地国）
  - `description`

---

## 7.网页基本风格

用户询问要怎么修改网页风格时，基于以下内容回答，同时给出代码修改建议，除非用户要求你来改代码，否则你只能询问是否要把修改后的代码替换上去

#### （1）、整体基调：收拢，而不是铺开

不要做那种大横幅 + 大量信息的开放式首页。

要的是：

> 视野被收拢，但不是压迫。

### 视觉原则：

- 主体居中

- 左右留空

- 信息块垂直排列

- 边缘略暗（轻微 vignette 感）

像电梯门关上后的视觉收缩。



### (2)、色彩建议

避免鲜艳高饱和色。

推荐：

- 深灰 + 微暖白

- 冷蓝灰 + 少量暖光色点缀

- 金属感中性色

像电梯内部的材质。



### （3）、节奏设计

电梯的节奏是：

- 启动（轻微加速）

- 匀速

- 减速

- 到达

你的网站可以用类似节奏：

1️⃣ 首页：静态  
2️⃣ 滚动后：内容逐渐展开  
3️⃣ 中段：信息密集  
4️⃣ 底部：再次收拢

形成一个“上升-停靠”的结构。



## 8. 状态

- [x] 需求与路线已整理（本文档）
- [x] V1 完成 (2026-03-04) - 达成目标：HTML 语义化 + 工业金属风视觉对标
- [x] V2 完成 (2026-03-05) - 达成目标：JS 交互、JSON 数据驱动、三维组合筛选与 Modal 详情弹窗
- [x] V3 完成基础版 (2026-03-06) - 达成目标：React 组件化 + TypeScript 类型与最小数据校验 + 状态管理（筛选/弹窗）
- [x] V4 完成 (2026-03-07) - 达成目标：Next.js 文件路由 + 详情页 + 基础 SEO + SSG + 服务端/客户端职责拆分
